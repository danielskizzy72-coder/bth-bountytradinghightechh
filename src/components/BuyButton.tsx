// Legacy BuyButton kept for compatibility.
// New cart functionality is handled via CartContext and ProductCard.
export function BuyButton({ productId, className = '' }: { productId: number; className?: string }) {
  return null
}
